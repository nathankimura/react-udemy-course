import { Paragraph } from '../P';
import { H1 } from '../H1';

// eslint-disable-next-line
export const Div = ({ children }) => {
  return (
    <div>
      <H1 />
      <Paragraph />
    </div>
  );
};
